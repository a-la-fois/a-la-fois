{
  "name": "@a-la-fois/doc-handler",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc",
    "start": "yarn build && node dist/main.js",
    "start:dev": "npm run build && nodemon --ext \".ts,.js\" --watch \"./src\" --exec \"ts-node ./src/index.ts\"",
    "start:dapr": "dapr run --app-id docHandler --app-protocol http --app-port 50001 --dapr-http-port 50000 --config ./dapr-config.yaml yarn start"

  },
  "dependencies": {
    "@a-la-fois/nest-common": "*",
    "@dapr/dapr": "2.3.1"
  }
}
